#include "monty.h"
/**
 * r_stack - prints the top
 * @head: stack head
 * @counter: line_number
 * Return: no return
*/
void r_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
